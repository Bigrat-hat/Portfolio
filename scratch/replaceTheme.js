const fs = require("fs"); const path = require("path"); const dir = "src/components"; fs.readdirSync(dir).forEach(file => { if(file.endsWith(".jsx")){ const filepath = path.join(dir, file); let content = fs.readFileSync(filepath, "utf8"); content = content.replace(/text-white/g, "text-textMain"); content = content.replace(/bg-white\//g, "bg-bgGlass/"); content = content.replace(/border-white\//g, "border-borderGlass/"); fs.writeFileSync(filepath, content); } });
